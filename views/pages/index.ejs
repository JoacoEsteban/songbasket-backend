<!DOCTYPE html>
<html>
<head>
    <link href="https://fonts.googleapis.com/css?family=Lato:300,400,700&display=swap" rel="stylesheet">  <style>
   
    html{
      font-size: 10px;
      text-align: center;
    }
    body{
      background: black;
      color: #ccc;
      font-size: 1.5em;
      font-family: Lato, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
      transition: all 1s;
    }
    div{
      margin:0;
      padding: 0;
    }
    input{
      background: black;
      color: #fff;

      border: .2rem solid #1AA34A;
      border-radius: 1em;

      margin: 0 .5rem;
      text-align: center;
      
      font-family: lato, monospace;
      font-size: 1em;
    }
    em{
      font-size: 1.5em;
    }

    
    
    
    h3{
      margin: .5em .5em 3em .5em;
      font-weight: 300;
    }
    h2{
      color: #1AA34A;
      display: inline-block;
      margin-block-start: 0;
      margin-block-end: 0;
      font-weight: 700;
    }
    
    
    button{
      margin: 0 1em;
      border: .2rem solid #1AA34A;

      background: none;

      font-family: Lato;
      color: #1AA34A;
      font-size: 1.5em;
      border-radius: 1em;
      transition: all .1s ease-in-out;
    }
    button:hover{
      transform: scale(1.1);
      color: white;
      background: #1AA34A;
      border-color: black;
    }
  </style>
</head>

  <body>
    <div>
      <h2>Artist</h2><button id="artist" >GO</button>
      <h3> <code>/artist/<input placeholder="your-artist" /> </code> to get a JSON response with the artist</h3>
    </div>
    <div>
      <h2>Top Tracks</h2><button id="topTracks" onclick="go()" >GO</button>
    <h3> <code>/artist/<input placeholder="your-artist-id" />/top-tracks</code> to get a JSON response with the artist Top 10 Tracks</h3>
    </div>
    <div>
      <h2>Artist & Top Tracks</h2><button id="artistNTracks" onclick="go()" >GO</button>
      <h3> <code>/artist-with-tracks/<input placeholder="your-artist" />/top-tracks</code> to get a JSON response with <em>BOTH</em> the artist and their Top 10 Tracks</h3>
    </div>
    <script >





      var btnArtist = document.getElementById('artist');
      var btnTopTracks = document.getElementById('topTracks');
      var btnArtistNTracks = document.getElementById('artistNTracks');

      console.log('btnArtist')
      btnArtist.addEventListener('click', ()=>{console.log('aqui')});
    
    
    
    
    
    </script>
  </body>

</html>
